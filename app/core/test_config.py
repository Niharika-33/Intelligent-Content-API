from app.core.config import Settings

# This is an in-memory SQLite URL for testing purposes
TEST_DATABASE_URL = "sqlite+aiosqlite:///:memory:"

# We only need this placeholder for now; the connection logic is managed in the fixture.